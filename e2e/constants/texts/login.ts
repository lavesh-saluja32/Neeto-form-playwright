/** Accessible names for login / post-login UI (roles, labels). */
export const LOGIN_UI_TEXTS = {
  /** Substring match for the avatar trigger in the floating action menu. */
  avatarMenuButtonName: "avatar",
} as const;
